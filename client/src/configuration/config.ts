export const serverAPIPort = 8080;
export const host = 'http://localhost'
export const APIPath = '/api/'
export const APIRootPath = `${host}:${serverAPIPort}${APIPath}`
export const booksAPIRootPath = `${APIRootPath}book/`
export const notesAPIRootPath = `${APIRootPath}note/`