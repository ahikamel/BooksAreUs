export type Book = {
    id: string;
    title: string;
    author: string;
    publishingDate: string;
    rating: number;
    image: string;
}

export type Note = {
    content: string;
    date: Date;
}